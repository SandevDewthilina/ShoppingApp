<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.javapoint.shoppingapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/gflock_name"
        android:label="@string/app_name"
        android:roundIcon="@drawable/gflock_logo_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EntranceActivity"></activity>
        <activity
            android:name=".ImageViewActivity"
            android:parentActivityName=".CollectionActivity" />
        <activity android:name=".CollectionActivity" />
        <activity
            android:name=".RegisterActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>